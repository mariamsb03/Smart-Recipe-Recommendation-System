name: CI/CD Pipeline - Phase 4

on:
  pull_request:
    branches: [ dev ]

  push:
    branches: [ dev, staging, main ]

# ======================================================
# 1️⃣ PR → dev : Integration tests only
# ======================================================
jobs:
  pr-integration-tests:
    name: PR Integration Tests
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    environment: PHASE4

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install backend dependencies
        run: |
          pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Run integration tests
        env:
          FLASK_ENV: ${{ secrets.FLASK_ENV }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
          MLFLOW_EXPERIMENT_NAME: ${{ secrets.MLFLOW_EXPERIMENT_NAME }}
        run: |
          pytest backend/tests/integration -v


# ======================================================
# 2️⃣ Push → dev : no tests, just validation placeholder
# ======================================================
  dev-push:
    name: Dev Branch Push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev'

    steps:
      - name: Dev push acknowledged
        run: |
          echo "✅ Code pushed to dev branch"
          echo "➡️ Open PR to staging when ready"


# ======================================================
# 3️⃣ Push → staging : ALL tests
# ======================================================
  staging-tests:
    name: Staging - Full Test Suite
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/staging'
    environment: PHASE4

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # -------- Backend --------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install backend dependencies
        run: |
          pip install -r backend/requirements.txt

      - name: Run backend unit tests
        env:
          FLASK_ENV: ${{ secrets.FLASK_ENV }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
        run: |
          pytest backend/tests/unit -v

      - name: Run backend integration tests
        env:
          FLASK_ENV: ${{ secrets.FLASK_ENV }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
        run: |
          pytest backend/tests/integration -v

      - name: Run end-to-end tests
        env:
          FLASK_ENV: ${{ secrets.FLASK_ENV }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
        run: |
          pytest backend/tests/e2e -v


# ======================================================
# 4️⃣ Push → main : Build & push ONE Docker image
# ======================================================
  build-and-push-docker:
    name: Production - Docker Build & Push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment: PHASE4

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/flavorfit
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/flavorfit:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/flavorfit:buildcache,mode=max

      - name: Image info
        run: |
          echo "✅ Docker image built and pushed"
          echo "📦 Image: ${{ secrets.DOCKER_USERNAME }}/flavorfit:latest"
          echo "🔖 SHA: ${{ github.sha }}"


# ======================================================
# 5️⃣ Deploy to Railway (Production)
# ======================================================
  deploy-railway:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: build-and-push-docker
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Railway Deployment
        run: |
          curl -X POST "${{ secrets.RAILWAY_DEPLOY_HOOK }}" \
            -H "Content-Type: application/json" \
            -d '{}'
        env:
          RAILWAY_DEPLOY_HOOK: ${{ secrets.RAILWAY_DEPLOY_HOOK }}

      - name: Wait for deployment
        run: |
          echo "⏳ Waiting for Railway to pull and deploy the new image..."
          sleep 30

      - name: Deployment triggered
        run: |
          echo "================================================"
          echo "🚂 Railway Deployment Triggered!"
          echo "================================================"
          echo "✅ Docker image: ${{ secrets.DOCKER_USERNAME }}/flavorfit:latest"
          echo "🌐 Check Railway dashboard for deployment status"
          echo "📍 Your app: https://your-app.up.railway.app"
          echo "================================================"


# ======================================================
# 6️⃣ Pipeline Summary
# ======================================================
  summary:
    name: Pipeline Summary
    runs-on: ubuntu-latest
    needs:
      - pr-integration-tests
      - staging-tests
      - build-and-push-docker
      - deploy-railway
    if: always()

    steps:
      - name: Summary
        run: |
          echo "================ PIPELINE SUMMARY ================"
          echo "PR Integration Tests: ${{ needs.pr-integration-tests.result }}"
          echo "Staging Tests:        ${{ needs.staging-tests.result }}"
          echo "Docker Build:         ${{ needs.build-and-push-docker.result }}"
          echo "Railway Deploy:       ${{ needs.deploy-railway.result }}"
          echo "=================================================="
          
          if [[ "${{ needs.build-and-push-docker.result }}" == "failure" ]] || \
             [[ "${{ needs.deploy-railway.result }}" == "failure" ]]; then
            echo "❌ Deployment pipeline failed"
            exit 1
          elif [[ "${{ needs.build-and-push-docker.result }}" == "success" ]] && \
               [[ "${{ needs.deploy-railway.result }}" == "success" ]]; then
            echo "✅ Deployment successful!"
            echo "🌐 Your app is live!"
          fi
